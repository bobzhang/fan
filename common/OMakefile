
OCAMLFLAGS = -w a


OCAMLINCLUDES += +compiler-libs

oconfig.ml: oconfig.mlp
	sed -e "s|%%LIBDIR%%|$(STDLIBPATH)|" $< > $@


OCAMLDEP_MODULES_ENABLED=false ## necessary!
OCamlGeneratedFiles(oconfig.ml)

COMMON_FILES[] =
	libUtil
	ast2pt
	oconfig
	astPrint
	ast_basic
	dyn_array
	fConfig
	fLoc
	fanTokenFilter
	fanUtil
	fan_warnings
	fcset
	fdir
	foptions
	fstream
	ftoken
	lex_util
	lexgen
	libUtil
	location_util
	parsetreeHelper
	tokenEval
	translate_lex

OCamlLibrary(libcommon, $(COMMON_FILES))

.PHONY: clean
clean:
	rm -rf *.cmo *.cmi

