


OCAMLINCLUDES += $(TREEPARSER_INCLUDES)
OCAMLFLAGS +=  -I +compiler-libs -w a  # for PreCast to communicate with toplevel 

OCamlLibrary(libmain, $(FAN_FILES))
OCamlLibrary(libmain_top, $(FAN_FILES) fanTop)
clean:
    rm -rf $(OCAMLBIN_EXT)
