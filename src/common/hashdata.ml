

(** pre-hash some existing hash_variants to accelerate compile time
    this file should be generated, since hashtbl needs to hash--lookup as well
 *)


(* let cache (string,int) Hashtbl.t = Hashtbl.create 100

    *)


(* let hash : (x:string) : int = *)
(*   if Hashtblf.find x  *)
