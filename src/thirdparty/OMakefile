
OCAMLPPFLAGS = -pp $(PP_FAN)
OCAMLDEPPPFLAGS = -pp $(PP_FAN)  # only useful for ml file 
NATIVE_ENABLED=true
NATIVEPLUGIN_ENABLED = true 
FILES = lang_error

.PHONY:lib clean
    
lib:$(OCamlLibrary libplugins, $(FILES))

clean:
    rm -rf $(OCAMLBIN_EXT)
    
# local variables:
# compile-command: "pmake lib"
# end: