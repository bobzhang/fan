
FAN_PLUGINS += $(OUNIT_PLUGIN)
OCAMLPPFLAGS = -pp $"$(PP_FAN) $(FAN_PLUGIN_OPTIONS)"

OCAMLDEPPPFLAGS = -pp $"$(PP_FAN) $(FAN_PLUGIN_OPTIONS)"
    # only useful for ml file 

section
    .SUBDIRS: main

.PHONY: test
test:.PHONY/main/test    