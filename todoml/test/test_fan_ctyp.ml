open Format;
#load "fan_ctyp.cma";
open Fan_ctyp;

(*
value f = mk_transform_type_eq ();

value v =
  (f#str_item
     <:str_item<
   type a = Loc.t
   and  b 'a  = [ A of LL.t 'a and LL.t 'a and Loc.t]; value f x = 3         
    >> );
*)
(* v |> opr#str_item fmt; *)


















